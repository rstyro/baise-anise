<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lrs.core.business.mapper.BizProductSkuMapper">

    <!--
        高并发下单库存扣减：
        使用单条条件 UPDATE 让 MySQL 行锁保证原子性，避免“先查库存再更新”导致超卖。
    -->
    <update id="decreaseStock">
        UPDATE biz_product_sku
        SET stock = stock - #{stockQuantity},
            sales = COALESCE(sales, 0) + #{salesQuantity},
            update_time = NOW()
        WHERE id = #{skuId}
          AND status = 1
          AND is_deleted = 0
          AND stock >= #{stockQuantity}
    </update>
</mapper>
